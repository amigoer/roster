{
  "name": "@roster/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && cp src/db/schema.sql dist/db/schema.sql",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm build && node --test \"test/*.test.mjs\""
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "1.4.0",
    "@roster/adapter-api": "workspace:*",
    "npm": "11.19.1",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@roster/ext-claude-code": "workspace:*",
    "@roster/ext-pi-agent": "workspace:*",
    "@types/node": "^22.19.0"
  }
}
