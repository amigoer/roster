{
  "name": "roster",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@10.11.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm --filter @roster/adapter-api build && pnpm --filter @roster/ext-claude-code build && pnpm --filter @roster/ext-pi-agent build && pnpm --filter @roster/core build && pnpm --filter @roster/ui build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm --filter @roster/adapter-api build && pnpm --filter @roster/ext-claude-code test && pnpm --filter @roster/ext-pi-agent build && pnpm --filter @roster/core test",
    "icons": "node scripts/icons.mjs",
    "package": "pnpm build && node scripts/package.mjs",
    "start": "ROSTER_EXTENSIONS=packages pnpm --filter @roster/desktop start",
    "core": "ROSTER_EXTENSIONS=packages node packages/core/dist/main.js"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron"
    ],
    "overrides": {
      "@anthropic-ai/claude-agent-sdk": "0.3.269"
    }
  }
}
